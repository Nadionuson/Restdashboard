/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: {}, // use empty object if you're enabling this
  },
  // Add other config options here if needed
};

export default nextConfig;
